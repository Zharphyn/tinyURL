<!DOCTYPE html>
<html lang="en">
<head>

</head>
<body>
  <header>
    <% include ./partials/_header %>
  </header>    <!-- Add your EJS code here -->
  <h2>URLS</h2>
<!--   <form action="/urls" method="POST" style="margin:150px;"> --> 
 	<ul>
     <% for (var key in urlDatabase) { %>
          <li>
          	<%= key + ': ' + urlDatabase[key] %>
            <form method= "post"  style= "display: inline;" action="/urls/<%= key %>/delete">
              <button type="submit">Delete</button>
            </form>
            <form method= "post"  style= "display: inline;" action="/urls/<%= key %>">
              <button type="submit">Update</button>
            </form>
            <form method= "post"  style= "display: inline;" action="/urls/u/<%= key %>">
              <button type="submit">Follow</button>
            </form>
          </li>
              <% } %>
  </ul>
<!-- </form> -->
  <footer>
    <% include ./partials/_footer %>
  </footer>
</body>
</html>