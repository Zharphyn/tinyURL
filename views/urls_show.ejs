<!DOCTYPE html>
<html lang="en">
<head>
  <style>
    input[type='text'] { font-size: 36px; }
    button[type='submit'] { font-size: 36px; }
  </style>
</head>
<body>
  <header>
    <% include ./partials/_header %>
  </header>
    <form method= "post" action="/urls/show/<%= shortURL %>" >
      <h2>Shortened URL : http://localhost:8080/u/<%= shortURL %> </h2>
      <input size = <%  if (longURL === undefined) { return 60; } else {return longURL.length;} %> id="longURL" type="text" name="name" value="<%= longURL %>">
      <button type="submit">Update</button>
    </form>
  <footer>
    <% include ./partials/_footer %>
  </footer>
</body>
</html>